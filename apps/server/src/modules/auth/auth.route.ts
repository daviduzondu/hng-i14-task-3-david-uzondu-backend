import { Router } from "express";

const router: Router = Router();

router.get("/github", () => {});
router.get("/github/callback")
router.post("/refresh")
router.post("/logout")

export default router;